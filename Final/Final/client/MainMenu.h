#include <menu.h>

#ifndef MAIN_MENU
#define MAIN_MENU


extern int load_menu (char*);
extern int load_list_of_files ();
extern WINDOW* contruct_window (int h, int w, int starty, int startx);

#endif
